{
  "name": "wakeword-firewall-tests",
  "private": true,
  "version": "0.1.0",
  "description": "测试用的依赖清单。扩展本身不需要 npm，装扩展也不需要——只有跑测试要。",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e && npm run test:ui",
    "test:unit": "node test/mp4-demux-test.js && node test/text2token-test.js",
    "test:e2e": "node test/e2e-selftest.js",
    "test:ui": "node test/bundled-test.js && node test/onboarding-test.js",
    "postinstall": "echo '还差一步浏览器：npx playwright install chromium'"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
